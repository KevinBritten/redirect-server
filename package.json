{
  "name": "redirect",
  "description": "redirect to a site using webpack dev server",
  "version": "1.0.0",
  "author": "",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development webpack-dev-server --hot --host 0.0.0.0 --port 3000",
    "devy": "cross-env NODE_ENV=development webpack-dev-server --hot --host 0.0.0.0 --port 3000 --open"
  },
  "devDependencies": {
    "cross-env": "^5.0.5",
    "file-loader": "^1.1.4",
    "webpack": "^3.6.0",
    "webpack-dev-server": "^2.9.1"
  }
}
